# 用户指定最终图—代码映射

整理日期：2026-09-13

本目录中的代码按用户指定的 5 张最终图逐张对应。图件副本位于 `05_图件/用户指定最终图/`。

| 最终图 | 对应代码 | 来源性质与核验说明 |
|---|---|---|
| 问题1_六时段储电量变化瀑布图.png | `plot_q1_soc_waterfall.py` | 当前工作区直接存在的出图脚本；脚本输出文件名与图件一致。 |
| fig5-7_四季典型日合并图_统一纵轴.png | `plot_typical_day_combined.py` | 根据项目会话记录中的原始创建内容恢复；该脚本调用 `plot_q23_contour.py` 的 `load_data`，但该依赖文件当前工作区未找到，因此本次只做代码归档和图件映射，未宣称重新运行。 |
| fig2_全年光伏_修正版.png | `rebuild_q23_G.py` | 当前工作区直接存在的出图脚本；脚本中的 `draw(pv, 'fig2_全年光伏_修正版', ...)` 与图件一致。 |
| fig3_全年负载_修正版.png | `rebuild_q23_G.py` | 当前工作区直接存在的出图脚本；脚本中的 `draw(load, 'fig3_全年负载_修正版', ...)` 与图件一致。 |
| fig4_全年净负荷_修正版.png | `rebuild_q23_G.py` | 当前工作区直接存在的出图脚本；脚本中的 `draw(net, 'fig4_全年净负荷_修正版', ...)` 与图件一致。 |

说明：本次保留的是“最终图 + 对应出图脚本”的最小支撑内容，没有把完整原始数据、重复图件或运行环境复制进来。脚本中的相对路径仍以原项目目录结构为准。
